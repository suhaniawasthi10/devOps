apiVersion: v1
kind: Service
metadata:
  name: webapp-nodeport
  namespace: devops-services
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
  - name: http
    port: 9090
    targetPort: http
    nodePort: 30090
