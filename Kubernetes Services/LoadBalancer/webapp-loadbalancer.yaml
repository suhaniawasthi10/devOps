apiVersion: v1
kind: Service
metadata:
  name: webapp-loadbalancer
  namespace: devops-services
spec:
  type: LoadBalancer
  selector:
    app: webapp
  ports:
  - name: http
    port: 9090
    targetPort: http
