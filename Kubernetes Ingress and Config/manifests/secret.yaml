# Dummy credentials for this local exercise only.
apiVersion: v1
kind: Secret
metadata:
  name: devops-db-secret
  namespace: devops-config
  labels:
    app: devops-app
type: Opaque
stringData:
  POSTGRES_USER: lab_user
  POSTGRES_PASSWORD: demo-only-password
  POSTGRES_DB: lab_db
